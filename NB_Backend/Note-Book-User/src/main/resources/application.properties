spring.application.name=NoteBook

# H2 (In-Memory / Testing) Properties
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
# spring.h2.console.settings.web-allow-others=false

# DataSource Configuration
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=name

# JPA/Hibernate Configuration
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# Spring Security UserName and Password
spring.security.user.name = shiva
spring.security.user.password = 12345

# Mail (required for JavaMailSender bean; set real values for sending)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shivaprasadpottabhatni@gmail.com
spring.mail.password=tshr ddqd lwty xzej
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT for email verification (min 32 chars for HS256). Override with env JWT_EMAIL_SECRET in production.
jwt.email.secret=${JWT_EMAIL_SECRET:NotebookEmailVerificationSecretKeyChangeInProduction32}